\chapter{2015年度（平成27年度）}
\label{ch:2015}

\section{第1期・専門基礎科目（3題必修）}

\begin{problem}{第1問｜基底と上三角化}{2015-b-1}
$V$ を有限次元実ベクトル空間とする。
\begin{enumerate}
  \item[(1)] 次の定義を述べよ。
  \begin{enumerate}
    \item[(i)] $\{w_1,w_2,\ldots,w_m\}\subset V$ が一次独立であること。
    \item[(ii)] $\{v_1,v_2,\ldots,v_n\}\subset V$ が $V$ の基底であること。
  \end{enumerate}
  \item[(2)] $\{v_1,\ldots,v_n\}$ を $V$ の基底とし，$f:V\to V$ を実ベクトル空間としての同型写像とする。このとき $\{f(v_1),\ldots,f(v_n)\}$ も $V$ の基底であることを示せ。
  \item[(3)] $V=\R^n$ として，$\{v_1,\ldots,v_n\}$ を $V$ の基底とする。各 $i$ $(1\le i\le n)$ に対して $W_i$ を $v_1,\ldots,v_i$ で生成される部分空間とする。$n$ 次正方行列 $A$ が与えられ，各 $i$ に対して $AW_i\subset W_i$ が成り立っているとする。このとき，$n$ 次正則行列 $P$ と $n$ 次上三角行列 $T$ が存在して $P^{-1}AP=T$ とできることを示せ。
\end{enumerate}
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
一次独立とは、線形結合が0になる方法が係数を全て0にする場合しかないこと、
基底とは、一次独立で空間全体を生成するベクトルの組である。
同型写像は線形な全単射である。線形性で一次結合を $f$ の内側へ入れ、
一次独立性には単射性を、生成することには全射性を使う。

上三角化では、基底ベクトルを列に並べた $P=(v_1\ \cdots\ v_n)$ を作る。
$P^{-1}AP$ は、線形変換 $A$ を基底 $v_1,\ldots,v_n$ の座標で表した行列である。
$Av_i\in\langle v_1,\ldots,v_i\rangle$ を係数表示すると、その係数が
$P^{-1}AP$ の第 $i$ 列になり、第 $i$ 成分より下が全て0になる。
\end{memo*}
\begin{proof}[解答]
\begin{enumerate}
\item[(1)] (i) $w_1,\ldots,w_m\in V$ が\textbf{一次独立}であるとは、
任意の $a_1,\ldots,a_m\in\R$ について
\[
\sum_{i=1}^m a_iw_i=0\quad\Longrightarrow\quad
a_1=\cdots=a_m=0
\]
が成り立つことをいう。

(ii) $v_1,\ldots,v_n\in V$ が $V$ の\textbf{基底}であるとは、$v_1,\ldots,v_n$ が一次独立であり、
かつ $V$ を生成する、すなわち任意の $v\in V$ に対して
ある $a_1,\ldots,a_n\in\R$ が存在して $v=\sum_{i=1}^n a_iv_i$ と書けることをいう。
$v_1,\ldots,v_k\in V$ の一次結合全体を $\langle v_1,\ldots,v_k\rangle$ と書けば、
生成することは $\langle v_1,\ldots,v_n\rangle=V$ と書ける。

\item[(2)] まず一次独立性を見る。$a_1,\ldots,a_n\in\R$ が
\[
a_1f(v_1)+\cdots+a_nf(v_n)=0
\]
を満たすとする。$f$ は線形写像なので左辺は $f(a_1v_1+\cdots+a_nv_n)$ に等しい。
また線形写像は $f(0)=0$ を満たすから
\[
f(a_1v_1+\cdots+a_nv_n)=f(0)
\]
である。$f$ は同型写像だから単射であり、従って
\[
a_1v_1+\cdots+a_nv_n=0
\]
となる。$v_1,\ldots,v_n$ は $V$ の基底なので一次独立であり、$a_1=\cdots=a_n=0$ を得る。
よって $f(v_1),\ldots,f(v_n)$ は一次独立である。

次に $V=\langle f(v_1),\ldots,f(v_n)\rangle$ を示す。任意の $v\in V$ を取る。
$f$ は同型写像だから全射であり、$v=f(w)$ となる $w\in V$ が存在する。
$w\in V$ で $v_1,\ldots,v_n$ は $V$ の基底だから、ある $a_1,\ldots,a_n\in\R$ を用いて
$w=a_1v_1+\cdots+a_nv_n$ と書ける。すると $f$ の線形性から
\[
v=f(a_1v_1+\cdots+a_nv_n)
=a_1f(v_1)+\cdots+a_nf(v_n)
\in\langle f(v_1),\ldots,f(v_n)\rangle
\]
である。$v\in V$ は任意だったので $V=\langle f(v_1),\ldots,f(v_n)\rangle$ であり、
$f(v_1),\ldots,f(v_n)$ は $V$ を生成する。

以上より $f(v_1),\ldots,f(v_n)$ は $V$ の基底である。

\item[(3)] $W_i=\langle v_1,\ldots,v_i\rangle$ とする。条件 $AW_i\subset W_i$ より、
特に $v_i\in W_i$ なので
\[
Av_i\in W_i=\langle v_1,\ldots,v_i\rangle
\]
である。従って、ある実数 $t_{1i},\ldots,t_{ii}\in\R$ が存在して
\[
Av_i=t_{1i}v_1+t_{2i}v_2+\cdots+t_{ii}v_i
\]
と書ける。

ここで、各 $v_i$ を列に並べた行列を
\[
P=(v_1\ \ v_2\ \cdots\ v_n)
\]
とする。$v_1,\ldots,v_n$ は $\R^n$ の基底なので $P$ は正則である。
また、上で得た係数を使って
\[
T=\begin{pmatrix}
t_{11}&t_{12}&t_{13}&\cdots&t_{1n}\\
0&t_{22}&t_{23}&\cdots&t_{2n}\\
0&0&t_{33}&\cdots&t_{3n}\\
\vdots&\vdots&&\ddots&\vdots\\
0&0&\cdots&0&t_{nn}
\end{pmatrix}
\]
とおく。これは上三角行列である。

行列 $AP$ の第 $i$ 列は $Av_i$ である。一方、$T$ の第 $i$ 列は
$(t_{1i},\ldots,t_{ii},0,\ldots,0)^{\mathsf T}$ なので、$PT$ の第 $i$ 列も
\[
t_{1i}v_1+\cdots+t_{ii}v_i=Av_i
\]
である。従って
\[
AP=PT
\]
が成り立つ。$P$ は正則なので、左から $P^{-1}$ を掛けると
\[
P^{-1}AP=T
\]
となる。よって $n$ 次正則行列 $P$ と $n$ 次上三角行列 $T$ が求める性質を満たす。
\end{enumerate}
\end{proof}

\begin{problem}{第2問｜広義積分}{2015-b-2}
$f_a(x)=x^a\log x$ $(x>0)$ とする。不定積分を求めよ。また
$I(a,R)=\iint_{x^2+y^2<R^2}f_a(1+x^2+y^2)\,\d x\d y$ とするとき $I(0,1)$ と，$\lim_{R\to\infty}I(a,R)$ が収束する $a$ の範囲を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
不定積分では $\log x$ を微分すると $1/x$ になり、$x^a$ は積分しやすいので部分積分を使う。
$\int x^a\,\d x=x^{a+1}/(a+1)$ には $a\ne-1$ が必要なため、$a=-1$ だけ別に計算する。

二重積分の領域は原点中心の円板で、被積分関数も $x^2+y^2$ だけで決まる。
そこで極座標を使うと面積要素は $\d x\d y=r\,\d r\d\theta$ となり、
角度積分は係数 $2\pi$ になる。$u=1+r^2$ と置けば
不定積分と同じ $\int u^a\log u\,\d u$ へ戻る。
最後は原始関数を上端へ代入し、有限な極限を持つ条件を判定する。
\end{memo*}
\begin{proof}[解答]
$a\ne-1$ なら部分積分で $u=\log x$, $\d v=x^a\,\d x$ とおくと
\[
\d u=\frac{\d x}{x},\qquad v=\frac{x^{a+1}}{a+1}
\]
なので
\begin{align*}
\int x^a\log x\,\d x
&=\frac{x^{a+1}\log x}{a+1}
-\frac1{a+1}\int x^a\,\d x\\
&=x^{a+1}\left(\frac{\log x}{a+1}-\frac1{(a+1)^2}\right)+C.
\end{align*}
$a=-1$ では $u=\log x$ と置換し、$\d u=\d x/x$ より
\[
\int\frac{\log x}{x}\,\d x=\int u\,\d u=\frac{(\log x)^2}{2}+C.
\]

二重積分では
\[
x=r\cos\theta,\qquad y=r\sin\theta
\]
とおく。このとき
\[
\frac{\partial(x,y)}{\partial(r,\theta)}
=\begin{vmatrix}
\cos\theta&-r\sin\theta\\
\sin\theta&r\cos\theta
\end{vmatrix}
=r(\cos^2\theta+\sin^2\theta)=r.
\]
円板 $x^2+y^2<R^2$ は $0\le r<R$, $0\le\theta<2\pi$ に対応するから
\begin{align*}
I(a,R)
&=\int_0^{2\pi}\int_0^R(1+r^2)^a\log(1+r^2)r\,\d r\d\theta\\
&=2\pi\int_0^R(1+r^2)^a\log(1+r^2)r\,\d r.
\end{align*}
$u=1+r^2$ とおけば $\d u=2r\,\d r$ なので
\[
I(a,R)=\pi\int_1^{1+R^2}u^a\log u\,\d u.
\]
$a=0$, $R=1$ のときは、上端が $1+R^2=2$ であり、原始関数の式へ $a=0$ を代入すると
\[
\int\log u\,\d u=u\left(\frac{\log u}{1}-\frac1{1}\right)+C=u\log u-u+C
\]
だから
\[
I(0,1)=\pi\left[u\log u-u\right]_1^2
=\pi\{(2\log2-2)-(1\cdot0-1)\}
=\pi(2\log2-1).
\]

$R\to\infty$ では上端 $1+R^2$ も $\infty$ へ向かうので、収束性は広義積分
$\int_1^\infty u^a\log u\,\d u$ の収束性で判定できる。
$a\ne-1$ のときの原始関数を
\[
\Phi(u)=u^{a+1}\left(\frac{\log u}{a+1}-\frac1{(a+1)^2}\right)
\]
と書く。$a<-1$ なら $a+1<0$ なので、$u\to\infty$ のとき $u^{a+1}\to0$ であり、また
\[
u^{a+1}\log u=\frac{\log u}{u^{-(a+1)}}\longrightarrow0
\]
である（分母の指数 $-(a+1)$ は正で、$\log u$ より速く増える）。従って $\Phi(u)\to0$ となり
\[
\int_1^\infty u^a\log u\,\d u
=0-\Phi(1)
=\frac1{(a+1)^2}
\]
と収束する。すなわち $\lim_{R\to\infty}I(a,R)=\pi/(a+1)^2$ である。

$a=-1$ なら原始関数は $(\log u)^2/2$ であり、$u\to\infty$ で $\infty$ へ発散する。
$a>-1$ なら、$u\ge e$ で $\log u\ge1$ だから
\[
\int_e^\infty u^a\log u\,\d u\ge\int_e^\infty u^a\,\d u
\]
であり、$a+1>0$ より右辺は発散する。従って左辺も発散する。
以上より、極限が収束するのは $a<-1$ の場合に限る。
\end{proof}

\begin{problem}{第3問｜二重周期関数}{2015-b-3}
連続関数 $f:\R^2\to\R$ が $f(x+1,y)=f(x,y+1)=f(x,y)$ を満たす。
$f(\R^2)=f([0,1]^2)$，$f$ が最大・最小値を持つこと，さらに最大値 $b$ と最小値 $a$ に対して $f\circ c:[0,1]\to[a,b]$ が全射となる連続曲線 $c$ が存在することを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
$\lfloor x\rfloor$ は $x$ 以下の最大の整数であり、$0\le x-\lfloor x\rfloor<1$ を満たす。
各座標から整数部分を引けば、周期性により関数値を変えずに任意の点を
基本領域 $[0,1]^2$ へ移せる。
$[0,1]^2$ は閉かつ有界なのでコンパクトであり、連続関数はそこで最大値・最小値を取る。

最小値を取る点 $p$ と最大値を取る点 $q$ を線分 $c(t)=(1-t)p+tq$ で結ぶ。
この線分は正方形の中に残り、一変数連続関数 $f(c(t))$ は端点で最小値と最大値を取る。
従って中間値の定理により、その間の全ての値を通る。
\end{memo*}
\begin{proof}[解答]
まず、仮定の周期性が整数だけ平行移動しても保たれることを見る。
$f(x+1,y)=f(x,y)$ を $x$ を $x+1,x+2,\ldots$ と取り替えて繰り返せば、
帰納法により全ての $m\ge0$ で $f(x+m,y)=f(x,y)$ である。
また $f(x,y)=f((x-1)+1,y)=f(x-1,y)$ なので、同じ議論が負の向きにも使える。
$y$ 方向も同様だから、全ての整数 $m,n$ について
\[
f(x+m,y+n)=f(x,y)
\]
が成り立つ。

$\lfloor x\rfloor$ を $x$ 以下の最大の整数とし、$m=\lfloor x\rfloor$, $n=\lfloor y\rfloor$ とおくと
$0\le x-m<1$, $0\le y-n<1$ なので
\[
(x-m,y-n)\in[0,1)^2\subset[0,1]^2
\]
であり、上の等式から $f(x,y)=f(x-m,y-n)$ である。
従って $\R^2$ 上に現れる値は全て $[0,1]^2$ 上でも現れ、
$f(\R^2)\subset f([0,1]^2)$ となる。一方、$[0,1]^2\subset\R^2$ だから
$f([0,1]^2)\subset f(\R^2)$ も成り立ち、両者は等しい。

$[0,1]^2$ は $\R^2$ の有界閉集合なので、Heine--Borel の定理によりコンパクトである。
$f$ は連続だから、最大値・最小値の定理により
\[
f(p)=a=\min_{[0,1]^2}f,\qquad
f(q)=b=\max_{[0,1]^2}f
\]
となる点 $p,q\in[0,1]^2$ が存在する。前段の $f(\R^2)=f([0,1]^2)$ により、
この $a,b$ は $\R^2$ 全体での最小値・最大値でもある。

$p$ と $q$ を結ぶ線分を
\[
c(t)=(1-t)p+tq\qquad(0\le t\le1)
\]
とおく。$[0,1]^2$ は凸集合なので $c(t)\in[0,1]^2$ であり、$c$ は $t$ の一次式だから連続である。
従って $f\circ c:[0,1]\to\R$ も連続で
\[
(f\circ c)(0)=f(p)=a,\qquad
(f\circ c)(1)=f(q)=b
\]
である。また $a,b$ は $f$ の最小値・最大値だから、$f\circ c$ の値は常に $[a,b]$ に入る。
任意の $y\in[a,b]$ に対して、中間値の定理から $(f\circ c)(t)=y$ となる
$t\in[0,1]$ が存在する。よって $f\circ c:[0,1]\to[a,b]$ は全射である。
\end{proof}

\section{専門科目（8題から2題選択）}

\begin{problem}{第1問｜有限体上の行列群}{2015-s-1}
$\mathbb F_p$ 上で
\[
G=\left\{\begin{pmatrix}a&0&b\\c&1&d\\e&0&f\end{pmatrix}\ \middle|\ af-be=1\right\}
\]
とする。$G$ が群であること，外側の $2\times2$ 小行列を取る写像 $\varphi:G\to GL_2(\mathbb F_p)$ が準同型であることを示せ。$K=\ker\varphi$ の中心化群を求め，さらに
$X=\begin{pmatrix}1&0&0\\0&1&0\\1&0&1\end{pmatrix}$ と $K$ が生成する $H$ の位数と中心を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
基底順を $(e_1,e_3;e_2)$ に並べ替えると、元は
線形部分 $B\in SL_2(\mathbb F_p)$ と平行移動部分 $u\in\mathbb F_p^2$ の組 $(B,u)$ になる。
この積公式で群・準同型・核をまとめて扱う。
中心化群では全ての $u$ を固定する $B$ を求め、
$H$ は $K$ と位数 $p$ の元による半直積として数える。
\end{memo*}
\begin{proof}[解答]
基底順を $(e_1,e_3;e_2)$ と見れば元は
$\begin{pmatrix}B&0\\u&1\end{pmatrix}$（$B\in SL_2,u\in\mathbb F_p^2$）と書ける。
この表示で積と逆元は
\[
\begin{pmatrix}B&0\\u&1\end{pmatrix}
\begin{pmatrix}C&0\\v&1\end{pmatrix}
=\begin{pmatrix}BC&0\\uC+v&1\end{pmatrix},
\]
\[
\begin{pmatrix}B&0\\u&1\end{pmatrix}^{-1}
=\begin{pmatrix}B^{-1}&0\\-uB^{-1}&1\end{pmatrix}.
\]
$BC,B^{-1}\in SL_2(\mathbb F_p)$ なので $G$ は群であり、
$\varphi(B,u)=B$ について $\varphi((B,u)(C,v))=BC=\varphi(B,u)\varphi(C,v)$ である。
$K=\{(I,u)\}\cong\mathbb F_p^2$ である。$(B,w)$ が全ての $(I,u)\in K$ と可換する条件を積の公式へ代入すると
\[
(B,w)(I,u)=(B,w+u),\qquad
(I,u)(B,w)=(B,uB+w).
\]
従って全ての行ベクトル $u$ について $uB=u$、すなわち $B=I$ が必要十分である。
よって $C_G(K)=K$。

$B_0=\varphi(X)=\begin{pmatrix}1&0\\1&1\end{pmatrix}$ とおくと
\[
B_0^j=\begin{pmatrix}1&0\\j&1\end{pmatrix}\qquad(j\in\mathbb F_p)
\]
なので $B_0$ の位数は $p$。$K\cap\langle X\rangle=\{1\}$ で、$X$ は共役により
$K$ を保つから、$H=K\rtimes\langle X\rangle$ であり
$|H|=p^2p=p^3$ となる。

中心の元は特に $K$ と可換するので、上の議論から $(I,u)$ の形に限る。
$u=(c,d)$ とすると
\[
(I,u)(B_0,0)=(B_0,uB_0),\qquad
(B_0,0)(I,u)=(B_0,u),
\]
かつ $uB_0=(c+d,d)$ だから、$X$ と可換する条件は $d=0$ である。従って中心は
\[
Z(H)=\left\{\begin{pmatrix}1&0&0\\c&1&0\\0&0&1\end{pmatrix}\middle|c\in\mathbb F_p\right\},
\]
従って位数 $p$。
\end{proof}

\begin{problem}{第2問｜整数係数多項式環}{2015-s-2}
$\Phi:\Z[X]\to\R\times\R$，$\Phi(f)=(f(\sqrt7),f(\sqrt{13}))$ とする。$f(\sqrt7)=0$ なら $X^2-7$ が $f$ を割ること，
$\ker\Phi=((X^2-7)(X^2-13))$ を示せ。$P_1=(\Phi(X^2-7))$，$P_2=(\Phi(X^2-13))$ が $\operatorname{Im}\Phi$ の素イデアルだが極大でないことを示し，両方を含む素イデアルの個数を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
$X^2-7$ で割った余りは一次式なので、$\sqrt7$ の無理性から余りが0と分かる。
この議論を $\sqrt{13}$ にも順に使って核を決める。
$P_1,P_2$ の素性は各成分への射影の商が整域になることから示す。
両方を含む素イデアルは差6も含むため、標数2と3に分けて因数分解を数える。
\end{memo*}
\begin{proof}[解答]
多項式の除法により
\[
f(X)=(X^2-7)q(X)+rX+s\qquad(r,s\in\Z)
\]
と書ける。$f(\sqrt7)=0$ なら $r\sqrt7+s=0$。もし $r\ne0$ なら
$\sqrt7=-s/r\in\Q$ となって矛盾するので $r=0$、さらに $s=0$ である。
従って $X^2-7$ は $f$ を割る。

$f\in\ker\Phi$ とする。上の結果から $f=(X^2-7)q$ と書ける。
$0=f(\sqrt{13})=6q(\sqrt{13})$ なので $q(\sqrt{13})=0$。
同じ議論により $X^2-13$ は $q$ を割るから
\[
f\in((X^2-7)(X^2-13)).
\]
逆にこの積の倍数は $\sqrt7$, $\sqrt{13}$ の両方で0になるので、
\[
\ker\Phi=((X^2-7)(X^2-13)).
\]

$\operatorname{Im}\Phi$ から第1成分を取る写像の像は $\Z[\sqrt7]$ で、核は
$P_1$ である。従って準同型定理から
\[
\operatorname{Im}\Phi/P_1\cong\Z[\sqrt7].
\]
これは整域なので $P_1$ は素イデアルだが、例えば2は逆元を持たないので体ではなく、
$P_1$ は極大でない。第2成分への射影
$\Z[\sqrt7]\times\Z[\sqrt{13}]\to\Z[\sqrt{13}]$
を $\Phi$ と合成した写像の核は $P_2$、像は $\Z[\sqrt{13}]$ である。従って
$\operatorname{Im}\Phi/P_2\cong\Z[\sqrt{13}]$ であり、$P_2$ も素だが極大でない。

$P_1,P_2$ の両方を含む素イデアル $\mathfrak p$ は
\[
\Phi(X^2-7)-\Phi(X^2-13)=\Phi(6)
\]
を含む。従って $6\in\mathfrak p$ であり、素性から $2\in\mathfrak p$ または
$3\in\mathfrak p$ である。法2では二つの関係式はいずれも
\[
X^2-1=(X-1)^2
\]
となるので素イデアルは $(2,X-1)$ に対応する1個。法3では
\[
X^2-1=(X-1)(X+1)
\]
となるので $(3,X-1)$ と $(3,X+1)$ に対応する2個である。
従って求める素イデアルは合計 $1+2=3$ 個である。
\end{proof}

\begin{problem}{第3問｜リフトと基本群}{2015-s-3}
弧状連結空間の写像 $f:Y\to X$ が道とホモトピーの一意なリフト性を持ち，$F=f^{-1}(x_0)$ は離散とする。誘導写像 $f_*:\pi_1(Y,y_0)\to\pi_1(X,x_0)$ が単射であることを示せ。また閉道のリフトの終点を対応させる $\varphi:\pi_1(X,x_0)\to F$ が well-defined，全射であり，$\varphi([\gamma_0])=\varphi([\gamma_1])$ と $[\gamma_0][\gamma_1]^{-1}\in f_*(\pi_1(Y,y_0))$ が同値であることを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
ホモトピーを持ち上げると、その端点はファイバー $F$ の中を連続に動く。
$F$ は離散、パラメータ区間は連結なので端点は動けない、という事実を繰り返し使う。
$f_*$ の単射性、$\varphi$ の代表元によらないこと、二つのリフトの終点条件を
全てこの一つの原理から示す。
\end{memo*}
\begin{proof}[解答]
まず $f_*$ の単射性を示す。$y_0$ を基点とする閉道 $\eta:[0,1]\to Y$ に対して
\[
f_*([\eta])=[f\circ\eta]=1
\]
と仮定する。このとき $f\circ\eta$ と定値道 $c_{x_0}$ の間に、端点を固定するホモトピー
\[
H:[0,1]\times[0,1]\to X
\]
が存在する。$H(0,t)=f(\eta(t))$ とし、このホモトピーを初期リフト $\eta$ から持ち上げて
$\widetilde H:[0,1]^2\to Y$ を得る。

各 $s\in[0,1]$ について $H(s,0)=H(s,1)=x_0$ だから
\[
\widetilde H(s,0),\ \widetilde H(s,1)\in F=f^{-1}(x_0).
\]
二つの写像 $s\mapsto\widetilde H(s,0)$ と $s\mapsto\widetilde H(s,1)$ は連続であり、
$[0,1]$ は連結、$F$ は離散なので、それぞれ一定である。$s=0$ では両端点が $y_0$ だから
\[
\widetilde H(s,0)=\widetilde H(s,1)=y_0
\qquad(0\le s\le1).
\]
さらに $H(1,t)=x_0$ であり、その $y_0$ から始まるリフトは道のリフトの一意性により
定値道 $c_{y_0}$ である。従って $\eta=\widetilde H(0,\cdot)$ は
$c_{y_0}=\widetilde H(1,\cdot)$ と端点を固定してホモトピックであり、$[\eta]=1$ である。
よって $\ker f_*=\{1\}$、すなわち $f_*$ は単射である。

次に、$x_0$ を基点とする閉道 $\gamma$ の $y_0$ から始まる一意なリフトを
$\widetilde\gamma$ とし
\[
\varphi([\gamma])=\widetilde\gamma(1)\in F
\]
と定める。同じ類を表す閉道 $\gamma_0,\gamma_1$ の間の端点固定ホモトピーを持ち上げると、
リフトの終点は $F$ の中を連続に動く。$F$ は離散なので終点は一定であり
\[
\widetilde\gamma_0(1)=\widetilde\gamma_1(1).
\]
従って $\varphi$ は代表元の取り方によらず well-defined である。

$\varphi$ が全射であることを示す。任意の $y\in F$ に対して、$Y$ の弧状連結性より
$y_0$ から $y$ への道 $\alpha$ を取れる。このとき
\[
\gamma=f\circ\alpha
\]
は $x_0$ を基点とする閉道であり、$\alpha$ 自身が $\gamma$ の $y_0$ から始まるリフトである。
従って $\varphi([\gamma])=\alpha(1)=y$ となり、$\varphi$ は全射である。

最後に、$\gamma_0,\gamma_1$ の $y_0$ から始まるリフトを
$\widetilde\gamma_0,\widetilde\gamma_1$ とする。もし
\[
\varphi([\gamma_0])=\varphi([\gamma_1])=:y
\]
なら、道
\[
\widetilde\gamma_0*\widetilde\gamma_1^{-1}
\]
は $y_0$ を基点とする $Y$ の閉道であり、その $f$ による像は
$\gamma_0*\gamma_1^{-1}$ である。従って
\[
[\gamma_0][\gamma_1]^{-1}
=f_*([\widetilde\gamma_0*\widetilde\gamma_1^{-1}])
\in f_*(\pi_1(Y,y_0)).
\]

逆に $[\gamma_0][\gamma_1]^{-1}\in f_*(\pi_1(Y,y_0))$ とする。
このとき $\gamma_0*\gamma_1^{-1}$ は、ある $Y$ の閉道の像と端点を固定してホモトピックである。
ホモトピーを持ち上げ、上と同じく終点が離散集合 $F$ の中で一定であることを使うと、
$\gamma_0*\gamma_1^{-1}$ の $y_0$ から始まるリフトも $y_0$ で終わる。
このリフトの後半を逆向きに見ると、それは $\gamma_1$ の $y_0$ から始まるリフトであり、
その終点は $\widetilde\gamma_0(1)$ である。道のリフトの一意性より、このリフトは
$\widetilde\gamma_1$ と一致する。従って
\[
\widetilde\gamma_0(1)=\widetilde\gamma_1(1),
\]
すなわち $\varphi([\gamma_0])=\varphi([\gamma_1])$ である。
\end{proof}

\begin{problem}{第4問｜楕円放物面}{2015-s-4}
$S$ を $z=2x^2+y^2$ のグラフ，$C(t)=(t,t,3t^2)$ とする。$S$ の Gauss 曲率・平均曲率，$C$ の接線方向の法曲率を求め，法曲率と平均曲率が等しい $C$ 上の点を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
グラフ表示から第1・第2基本形式の係数を順に計算する。
Gauss 曲率と平均曲率はこれらの標準公式へ代入する。
曲線 $C(t)=X(t,t)$ の座標速度は $(1,1)$ なので、
法曲率は第2基本形式と第1基本形式の比で求まる。
最後は $x=y=t$ を代入し、分母が正であることを確認して方程式を整理する。
\end{memo*}
\begin{proof}[解答]
グラフを $X(x,y)=(x,y,2x^2+y^2)$ とパラメータ表示する。
\[
X_x=(1,0,4x),\qquad X_y=(0,1,2y)
\]
なので、第1基本形式の係数と上向き単位法線は
\[
E=1+16x^2,\quad F=8xy,\quad G=1+4y^2,
\]
\[
n=\frac{(-4x,-2y,1)}W,\qquad W=\sqrt{1+16x^2+4y^2}.
\]
また $X_{xx}=(0,0,4)$, $X_{xy}=0$, $X_{yy}=(0,0,2)$ より
第2基本形式の係数は
\[
e=\frac4W,\qquad f=0,\qquad g=\frac2W.
\]
従って
\begin{align*}
K&=\frac{eg-f^2}{EG-F^2}
=\frac{8/W^2}{W^2}=\frac8{W^4},\\
H&=\frac{eG-2fF+gE}{2(EG-F^2)}\\
&=\frac{4(1+4y^2)+2(1+16x^2)}{2W^3}
=\frac{3+16x^2+8y^2}{W^3}.
\end{align*}
すなわち
\[
K=\frac8{W^4},\qquad H=\frac{3+16x^2+8y^2}{W^3}.
\]
$C(t)=X(t,t)$ の座標方向は $(\d x,\d y)=(1,1)$ なので、法曲率は
第2基本形式と第1基本形式の比から
\begin{align*}
k_n(t)
&=\frac{e+2f+g}{E+2F+G}\bigg|_{(x,y)=(t,t)}\\
&=\frac{6/\sqrt{1+20t^2}}{2+36t^2}\\
&=\frac6{\sqrt{1+20t^2}(2+36t^2)}.
\end{align*}
従って
\[
k_n(t)=\frac6{\sqrt{1+20t^2}(2+36t^2)}.
\]
$x=y=t$ を代入すると
\[
H(t,t)=\frac{3+24t^2}{(1+20t^2)^{3/2}},\qquad
k_n(t)=\frac6{\sqrt{1+20t^2}(2+36t^2)}.
\]
両辺に正の $(1+20t^2)^{3/2}(2+36t^2)$ を掛けて整理すると
\begin{align*}
6(1+20t^2)&=(3+24t^2)(2+36t^2)\\
&=6+156t^2+864t^4,
\end{align*}
すなわち $36t^2(1+24t^2)=0$。実数では $t=0$ だけなので、点は $(0,0,0)$ のみ。
\end{proof}

\begin{problem}{第5問｜3次方程式}{2015-s-5}
$f(z)=z^3+az+b$，$R=\max\{|a|,|b|\}+1$ とする。$|z|\ge R+1$ なら $f(z)\ne0$ を示せ。また
\[
P_n=\frac1{2\pi i}\int_{|z|=R+1}\frac{z^n(3z^2+a)}{z^3+az+b}\,\d z,
\quad \Delta=P_0P_3^2-\frac12P_2^3
\]
を $a,b$ で表し，$\Delta=0$, $(a,b)\ne(0,0)$ のときの全ての根を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
大円上では最高次項 $z^3$ が $az+b$ より大きいことを示し、全ての根を円内に入れる。
$f'/f$ の積分は偏角の原理により根の冪和 $P_n=\sum\alpha_j^n$ になる。
Vieta の公式で $P_2,P_3$ を $a,b$ に直し、
$\Delta=0$ では重根候補 $r=-3b/(2a)$ を作って多項式を因数分解する。
\end{memo*}
\begin{proof}[解答]
$M=\max\{|a|,|b|\}$ とおくと $R=M+1$ である。$t=|z|\ge R+1=M+2$ なら
\[
|az+b|\le |a|t+|b|\le M(t+1)\le(t-2)(t+1)<t^3=|z^3|.
\]
従って逆三角不等式から
$|f(z)|\ge |z|^3-|az+b|>0$ となり、$f(z)\ne0$ である。

$f'(z)=3z^2+a$ なので、偏角原理（対数微分の留数）から
\[
P_n=\frac1{2\pi i}\int_Cz^n\frac{f'(z)}{f(z)}\,\d z
=\sum_{j=1}^3\alpha_j^n,
\]
ここで $\alpha_1,\alpha_2,\alpha_3$ は重複度を込めた3根である。
$P_0=3$ であり、$\alpha_1+\alpha_2+\alpha_3=0$、
$\alpha_1\alpha_2+\alpha_2\alpha_3+\alpha_3\alpha_1=a$、
$\alpha_1\alpha_2\alpha_3=-b$ を使うと
\begin{align*}
P_2&=(\alpha_1+\alpha_2+\alpha_3)^2
-2(\alpha_1\alpha_2+\alpha_2\alpha_3+\alpha_3\alpha_1)=-2a,\\
P_3&=(\alpha_1+\alpha_2+\alpha_3)^3
-3(\alpha_1+\alpha_2+\alpha_3)
(\alpha_1\alpha_2+\alpha_2\alpha_3+\alpha_3\alpha_1)
+3\alpha_1\alpha_2\alpha_3=-3b.
\end{align*}
従って
\[
\Delta=3(-3b)^2-\frac12(-2a)^3=4a^3+27b^2.
\]

$\Delta=0$ かつ $(a,b)\ne(0,0)$ なら $a\ne0$ である。
$r=-3b/(2a)$ とおく。$4a^3+27b^2=0$ を用いると
\[
-3r^2=a,\qquad 2r^3=b.
\]
従って係数を比較して
\[
(z-r)^2(z+2r)=z^3-3r^2z+2r^3=z^3+az+b=f(z).
\]
よって根は $r=-3b/(2a)$（重複度2）と $-2r=3b/a$（重複度1）である。
\end{proof}

\begin{problem}{第6問｜常微分方程式}{2015-s-6}
$y'=\sin y$, $y(0)=\pi/2$ を解け。また $f\in C^1(\R^2)$ に対する $y'=\sin f(x,y)$ の任意の極大解の定義域が $\R$ 全体であることを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
初めの方程式は変数分離し、$1/\sin y$ の原始関数を
$\log\tan(y/2)$ として求める。
一般の方程式では右辺の絶対値が1以下なので、解は有限時間に高々その時間幅しか動かない。
従って有限端点で発散できず、局所存在・一意性定理によりさらに延長できる。
\end{memo*}
\begin{proof}[解答]
$0<y<\pi$ の範囲で変数分離すると
\[
\frac{\d y}{\sin y}=\d x,\qquad
\int\frac{\d y}{\sin y}=\log\tan\frac y2.
\]
ここで原始関数は
\[
\frac{\d}{\d y}\log\tan\frac y2
=\frac{1}{\tan(y/2)}\cdot\frac12\sec^2\frac y2
=\frac1{2\sin(y/2)\cos(y/2)}
=\frac1{\sin y}
\]
から確かめられる。
従って $\log\tan(y/2)=x+C$。初期条件 $y(0)=\pi/2$ から $C=0$ なので
\[
\tan\frac y2=e^x,\qquad y(x)=2\arctan(e^x).
\]
一般の式 $y'=\sin f(x,y)$ では $|y'|\le1$。解が $x_0$ から有限時刻 $b$ まで存在するとき
\[
|y(x)-y(x_0)|\le|x-x_0|
\]
だから $y$ は有限区間で発散しない。右辺は $C^1$ なので局所存在・一意性定理により
有限な端点の近くからさらに解を延長できる。従って極大解の定義域の端点は有限にならず、定義域は $\R$ 全体である。
\end{proof}

\begin{problem}{第7問｜収束定理}{2015-s-7}
$p>0$ を定数とする。測度空間 $(X,\mathfrak B,\mu)$ 上の $p$ 乗可積分関数 $f$ は
$0<f<\infty$ かつ $\int_Xf^p\,\d\mu=1$ を満たしているとする。このとき次の値を求めよ。
\[
\lim_{n\to\infty}\int_X n\left|\sin\left(\frac{f(x)}n\right)\right|^p\,\d\mu(x).
\]
（収束定理を使う場合はその根拠を述べよ。）
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
被積分関数の $n$ の効き方を先に取り出す。$u_n=f(x)/n$ とおくと
$|\sin u_n|^p=u_n^p(|\sin u_n|/u_n)^p$ であり、$u_n^p=f(x)^p/n^p$ だから
被積分関数は $n^{1-p}f(x)^p$ に、1へ収束する因子を掛けた形になる。
従って $n^{1-p}$ の挙動、すなわち $p$ と1の大小で結論が三つに分かれる。

$|\sin u|\le|u|$ による上からの評価 $n|\sin u_n|^p\le n^{1-p}f^p$ は、
$p>1$ ならそのまま積分して0を与える。$p=1$ ではこの評価が可積分な優関数 $f$ を与えるので
優収束定理が使える。$p<1$ では被積分関数が各点で $\infty$ へ発散するので、
下から評価する Fatou の補題を使う。
\end{memo*}
\begin{proof}[解答]
各 $x\in X$ について $u_n=f(x)/n$ とおく。$0<f(x)<\infty$ だから $u_n>0$ かつ $u_n\to0$ であり、
$\sin u/u\to1$ $(u\to0)$ より
\[
\frac{|\sin u_n|}{u_n}\longrightarrow1.
\]
$|\sin u_n|^p=u_n^p\left(|\sin u_n|/u_n\right)^p$ と $u_n^p=f(x)^p/n^p$ を使うと
\[
n\left|\sin\left(\frac{f(x)}n\right)\right|^p
=n^{1-p}f(x)^p
\left(\frac{|\sin u_n|}{u_n}\right)^p
\tag{$*$}
\]
であり、右端の因子は各点で1へ収束する。
また $u\ge0$ で $|\sin u|\le u$ だから
\[
0\le n\left|\sin\left(\frac{f(x)}n\right)\right|^p
\le n\,u_n^p=n^{1-p}f(x)^p
\tag{$**$}
\]
が全ての $n$ と $x$ で成り立つ。以下、$p$ と1の大小で場合を分ける。

$p>1$ のとき。式 ($**$) を積分し、仮定 $\int_Xf^p\,\d\mu=1$ を使うと
\[
0\le\int_X n\left|\sin\left(\frac{f(x)}n\right)\right|^p\,\d\mu
\le n^{1-p}\int_Xf^p\,\d\mu=n^{1-p}.
\]
$1-p<0$ だから $n^{1-p}\to0$ であり、はさみうちにより極限は $0$ である。

$p=1$ のとき。式 ($**$) は $n|\sin(f(x)/n)|\le f(x)$ となり、
$\int_Xf\,\d\mu=1<\infty$ だから $f$ は可積分な優関数である。
一方 式 ($*$) で $n^{1-p}=1$ なので、被積分関数は各点で $f(x)$ へ収束する。
よって優収束定理を適用でき
\[
\lim_{n\to\infty}\int_X n\left|\sin\left(\frac{f(x)}n\right)\right|\,\d\mu
=\int_Xf\,\d\mu=1.
\]

$0<p<1$ のとき。$1-p>0$ だから $n^{1-p}\to\infty$ であり、$f(x)^p>0$ と
式 ($*$) から、被積分関数は各点で $\infty$ へ発散する。
被積分関数は非負なので Fatou の補題が使えて
\[
\liminf_{n\to\infty}\int_X n\left|\sin\left(\frac{f(x)}n\right)\right|^p\,\d\mu
\ge\int_X\liminf_{n\to\infty}
n\left|\sin\left(\frac{f(x)}n\right)\right|^p\,\d\mu
=\int_X\infty\,\d\mu.
\]
ここで $\int_Xf^p\,\d\mu=1\ne0$ より $\mu(X)>0$ だから右辺は $\infty$ である。
従って極限は $+\infty$ である。

以上をまとめると、求める極限は
\[
\lim_{n\to\infty}\int_X n\left|\sin\left(\frac{f(x)}n\right)\right|^p\,\d\mu
=\begin{cases}
+\infty,&0<p<1,\\
1,&p=1,\\
0,&p>1
\end{cases}
\]
である。
\end{proof}

\begin{problem}{第8問｜独立確率変数の級数}{2015-s-8}
$X$ の平均を $\mu$，分散を $\sigma^2$ とし，独立な $X_n$ の密度を $nf(nx)$ とする。$S_n=\sum_{k=1}^nX_k$ とおく。$E[X_n],V[X_n]$ を求め，$S_n$ が概収束する必要十分条件が $\mu=0$ であることを示せ。$X$ が標準正規分布なら極限分布を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
密度の変数変換から $X_n$ は $X/n$ と同分布だと読み取る。
平均を引いた $Y_n=X_n-\mu/n$ は分散の総和が有限なので、その級数は概収束する。
残る平均部分は調和級数であり、$\mu=0$ かどうかを判定する。
標準正規の場合は独立な正規分布の和なので、分散の極限から極限分布が分かる。
\end{memo*}
\begin{proof}[解答]
$X_n$ の分布関数を変数変換すると、任意の有界可測関数 $\psi$ に対し
\[
E[\psi(X_n)]=\int_{\R}\psi(x)nf(nx)\,\d x
=\int_{\R}\psi(u/n)f(u)\,\d u.
\]
従って $X_n$ は $X/n$ と同分布であり
\[
E[X_n]=\frac\mu n,\qquad V[X_n]=\frac{\sigma^2}{n^2}.
\]

$Y_n=X_n-\mu/n$ とおくと、$Y_n$ は独立、平均0で
\[
\sum_{n=1}^\infty V[Y_n]
=\sigma^2\sum_{n=1}^\infty\frac1{n^2}<\infty.
\]
Kolmogorov の収束定理により $\sum_nY_n$ はほとんど確実に収束する。
従って
\[
S_n=\sum_{k=1}^nY_k+\mu\sum_{k=1}^n\frac1k.
\]
$\mu=0$ なら右辺第1項だけなので $S_n$ は概収束する。
$\mu\ne0$ なら第1項は有限値へ収束する一方、調和級数を含む第2項は
$\operatorname{sgn}(\mu)\infty$ へ発散するため、$S_n$ は有限な確率変数へ概収束しない。

$X$ が標準正規分布なら $X_n\sim N(0,1/n^2)$ であり、独立性から
\[
S_n\sim N\left(0,\sum_{k=1}^n\frac1{k^2}\right).
\]
分散は $\sum_{k=1}^n1/k^2\to\pi^2/6$ だから、概収束極限の分布は
$N(0,\pi^2/6)$ である。
\end{proof}

\section{第2期・専門基礎科目（3題必修）}

\begin{problem}{第1問｜像・核と直和}{2015-2-1}
\[
A=\begin{pmatrix}1&2&3&0\\-1&0&-1&2\\2&3&5&-1\\1&1&2&-1\end{pmatrix},\quad
B=\begin{pmatrix}1&2&3&0\\1&2&2&1\\2&4&3&3\\3&6&7&2\end{pmatrix}.
\]
$\operatorname{Im}A,\operatorname{Im}B$ の基底と共通部分，$\dim(\ker A+\ker B)$ を求めよ。また一般に $\R^n=\operatorname{Im}f\oplus\operatorname{Im}g$ のとき $\ker(f+g)=\ker f\cap\ker g$ を示し，さらに $\R^n=\ker f+\ker g$ なら $f+g$ が全単射であることを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
行列の像は列ベクトルの全ての線形結合、核は $Ax=0$ を満たすベクトル全体である。
まず各列を少数の独立な列の線形結合として式で表し、その独立な列を像の基底にする。
共通部分のベクトルは $A$ 側と $B$ 側の二通りに基底表示できるため、二表示を等置して係数を解く。
核については、列の線形関係を $Ax$ の式へ代入して連立方程式を作る。

$U\oplus W$ は $U+W$ に加えて $U\cap W=\{0\}$ を満たす直和を意味する。
従って $f(x)=-g(x)$ が両方の像に属すれば0でなければならない。
最後は部分空間の和の次元公式を書き下し、$\ker(f+g)=\{0\}$、すなわち単射を示す。
同じ有限次元の空間同士では単射と全射が同値なので、全単射が従う。
\end{memo*}
\begin{proof}[解答]
列ベクトルを調べる。$A$ の第1、2列を
\[
a_1=(1,-1,2,1)^{\mathsf T},\qquad
a_2=(2,0,3,1)^{\mathsf T}
\]
とおくと、第3列は $a_1+a_2$、第4列は $-2a_1+a_2$ である。
$a_1,a_2$ は一次独立なので
\[
\operatorname{Im}A=\langle(1,-1,2,1),(2,0,3,1)\rangle,
\]
となる。次に $B$ の第1、3列を
\[
b_1=(1,1,2,3)^{\mathsf T},\qquad
b_2=(3,2,3,7)^{\mathsf T}
\]
とおくと、第2列は $2b_1$、第4列は $3b_1-b_2$ だから
\[
\operatorname{Im}B=\langle(1,1,2,3),(3,2,3,7)\rangle,
\]
である。共通部分の元を
$\alpha a_1+\beta a_2=\gamma b_1+\delta b_2$ と表す。4成分を比較すると
\[
\begin{cases}
\alpha+2\beta=\gamma+3\delta,\\
-\alpha=\gamma+2\delta,\\
2\alpha+3\beta=2\gamma+3\delta,\\
\alpha+\beta=3\gamma+7\delta.
\end{cases}
\]
第2式から $\gamma=-\alpha-2\delta$。これを第1、3、4式へ代入すると
\[
\delta=2\alpha+2\beta,\qquad
4\alpha+3\beta+\delta=0,
\qquad4\alpha+\beta-\delta=0.
\]
さらに $\delta=2\alpha+2\beta$ を後ろ2式へ代入して
\[
6\alpha+5\beta=0,\qquad 2\alpha-\beta=0
\]
を得る。第2式から $\beta=2\alpha$、これを第1式へ入れると
$16\alpha=0$ なので $\alpha=\beta=0$。従って
$\delta=0$, $\gamma=0$ であり
\[
\operatorname{Im}A\cap\operatorname{Im}B=\{0\}.
\]
列の関係式から、任意の $x=(x_1,x_2,x_3,x_4)^{\mathsf T}$ に対し
\[
Ax=(x_1+x_3-2x_4)a_1+(x_2+x_3+x_4)a_2.
\]
$a_1,a_2$ は一次独立だから $Ax=0$ は
\[
x_1+x_3-2x_4=0,\qquad x_2+x_3+x_4=0
\]
と同値である。$B$ についても列の一次関係を代入すると
\[
Bx=(x_1+2x_2+3x_4)b_1+(x_3-x_4)b_2
\]
だから、$Bx=0$ は
\[
x_1+2x_2+3x_4=0,\qquad x_3-x_4=0
\]
と同値である。従って
\[
\ker A=\langle(-1,-1,1,0),(2,-1,0,1)\rangle,
\]
\[
\ker B=\langle(-2,1,0,0),(-3,0,1,1)\rangle.
\]
ここで
\[
p=(-1,-1,1,0),\quad q=(2,-1,0,1),\quad
r=(-2,1,0,0),\quad s=(-3,0,1,1)
\]
とおくと
\[
s=p+q+2r
\]
である。従って $\ker A+\ker B=\langle p,q,r\rangle$ である。
さらに $ap+bq+cr=0$ とすると、第3、第4成分から
\[
a=0,\qquad b=0
\]
となり、第1成分から $-2c=0$、すなわち $c=0$ となる。
よって $p,q,r$ は一次独立であり
\[
\dim(\ker A+\ker B)=3
\]
である。

次に一般の線形写像 $f,g:\R^n\to\R^n$ を考え
\[
\R^n=\operatorname{Im}f\oplus\operatorname{Im}g
\]
と仮定する。この仮定から
\[
\operatorname{Im}f\cap\operatorname{Im}g=\{0\}
\]
である。$x\in\ker(f+g)$ なら
\[
f(x)+g(x)=0,
\qquad f(x)=-g(x).
\]
ここで $f(x)\in\operatorname{Im}f$ であり、同時に
$f(x)=-g(x)\in\operatorname{Im}g$ だから
\[
f(x)\in\operatorname{Im}f\cap\operatorname{Im}g=\{0\}.
\]
従って $f(x)=g(x)=0$ となり、$x\in\ker f\cap\ker g$ である。
逆に $x\in\ker f\cap\ker g$ なら
$(f+g)(x)=f(x)+g(x)=0$ だから
\[
\ker(f+g)=\ker f\cap\ker g
\]
を得る。

さらに $\R^n=\ker f+\ker g$ と仮定する。
有限次元の線形写像 $h:\R^n\to\R^n$ を取る。
$\ker h$ の基底を $e_1,\ldots,e_k$ とし、これにベクトルを加えて
$e_1,\ldots,e_n$ を $\R^n$ の基底にすると、
\[
h(e_{k+1}),\ldots,h(e_n)
\]
が $\operatorname{Im}h$ の基底になる。従って
\[
\dim\operatorname{Im}h=n-\dim\ker h.
\]
この等式を階数・退化次数の定理という。

像が直和で $\R^n$ 全体になるという仮定と、この等式から
\begin{align*}
n
&=\dim\operatorname{Im}f+\dim\operatorname{Im}g\\
&=(n-\dim\ker f)+(n-\dim\ker g),
\end{align*}
従って
\[
\dim\ker f+\dim\ker g=n.
\]
一方、核の和が $\R^n$ 全体であることと部分空間の和の次元公式から
\begin{align*}
n
&=\dim(\ker f+\ker g)\\
&=\dim\ker f+\dim\ker g-\dim(\ker f\cap\ker g)\\
&=n-\dim(\ker f\cap\ker g).
\end{align*}
よって $\ker f\cap\ker g=\{0\}$ である。先に示した核の等式より
\[
\ker(f+g)=\{0\},
\]
従って $f+g$ は単射である。$f+g$ は同じ有限次元空間
$\R^n$ から $\R^n$ への線形写像である。
$\ker(f+g)=\{0\}$ を上の次元の等式へ代入すると
\[
\dim\operatorname{Im}(f+g)=n-0=n.
\]
$\R^n$ の中の $n$ 次元部分空間は $\R^n$ 自身なので
$\operatorname{Im}(f+g)=\R^n$、すなわち $f+g$ は全射である。
以上から $f+g$ は全単射である。
\end{proof}

\begin{problem}{第2問｜滑らかな関数の分解}{2015-2-2}
$f\in C^\infty(\R^2)$，$f(0,0)=0$ とする。$f=xg+yh$ となる滑らかな $g,h$ があること，さらに $f=xG(x,y)+yH(y)$ とできることを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
目標の $f=xg+yh$ では係数 $x,y$ を作る必要がある。
そこで原点から $(x,y)$ へ向かう線分 $(tx,ty)$ を考え、
$\varphi(t)=f(tx,ty)$ を連鎖律で微分する。すると
$\varphi'(t)=xf_x(tx,ty)+yf_y(tx,ty)$ となり、必要な $x,y$ が現れる。
$t=0$ から1まで積分すれば、微積分学の基本定理により $f(x,y)-f(0,0)$ が戻る。

二つ目では $H$ を $y$ だけの関数にする必要がある。そのため
$f(x,y)-f(0,y)$ を $x$ 方向だけに積分し、残る $f(0,y)-f(0,0)$ を
$y$ 方向だけに積分する。
\end{memo*}
\begin{proof}[解答]
まず一変数関数 $\varphi(t)=f(tx,ty)$ を考える。連鎖律より
\[
\varphi'(t)=x f_x(tx,ty)+y f_y(tx,ty)
\]
である。$f(0,0)=0$ と微積分学の基本定理を用いると
\begin{align*}
f(x,y)
&=f(x,y)-f(0,0)\\
&=\varphi(1)-\varphi(0)\\
&=\int_0^1\varphi'(t)\,\d t\\
&=x\int_0^1f_x(tx,ty)\,\d t
 +y\int_0^1f_y(tx,ty)\,\d t.
\end{align*}
従って
\[
g(x,y)=\int_0^1f_x(tx,ty)\,\d t,
\qquad
h(x,y)=\int_0^1f_y(tx,ty)\,\d t
\]
とおけば $f=xg+yh$ となる。$f_x,f_y$ は滑らかであり、コンパクト区間 $0\le t\le1$ 上の
パラメータ積分は任意の階数で積分記号下に微分できるので、$g,h$ も滑らかである。

次の分解では
\[
f(x,y)=\{f(x,y)-f(0,y)\}+f(0,y)
\]
と二つに分ける。固定した $y$ に対して $s\mapsto f(sx,y)$ に基本定理を使うと
\begin{align*}
f(x,y)-f(0,y)
&=\int_0^1\frac{\d}{\d s}f(sx,y)\,\d s\\
&=x\int_0^1f_x(sx,y)\,\d s.
\end{align*}
また $f(0,0)=0$ だから、$t\mapsto f(0,ty)$ に同じ計算を行うと
\begin{align*}
f(0,y)
&=f(0,y)-f(0,0)\\
&=\int_0^1\frac{\d}{\d t}f(0,ty)\,\d t\\
&=y\int_0^1f_y(0,ty)\,\d t.
\end{align*}
そこで
\[
G(x,y)=\int_0^1f_x(sx,y)\,\d s,
\qquad
H(y)=\int_0^1f_y(0,ty)\,\d t
\]
とおけば、$G,H$ は滑らかであり
\[
f(x,y)=xG(x,y)+yH(y)
\]
を得る。
\end{proof}

\begin{problem}{第3問｜コンパクト性と連結性}{2015-2-3}
コンパクト空間 $S$ から Hausdorff 空間 $T$ への連続写像 $f$ と $p\in f(S)$ に対し $f^{-1}(\{p\})$ がコンパクトであることを示せ。また $X=\Q^2$，$Y=\R^2\setminus\Q^2$ は連結か判定せよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
ファイバー $f^{-1}(\{p\})$ は、$p$ へ写る点を全て集めた集合である。
Hausdorff 空間では一点集合が閉、連続写像では閉集合の逆像が閉、
コンパクト空間の閉部分集合はコンパクト、という三段階を順に使う。

$\Q^2$ の非連結性は、$x$ 座標がある無理数 $c$ より小さい側と大きい側に分けて示す。
$c$ 自身は有理点に現れないため二集合で全体を覆える。
$Y=\R^2\setminus\Q^2$ では、各点は少なくとも一方の座標が無理数である。
その無理数座標を固定した縦線または横線は $Y$ の外へ出ないので、折れ線で共通点へ結べる。
\end{memo*}
\begin{proof}[解答]
まず Hausdorff 空間 $T$ では一点集合 $\{p\}$ が閉である。実際、$q\ne p$ を取ると
Hausdorff 性から交わらない開近傍 $U\ni p$, $V\ni q$ が存在し、
$V\subset T\setminus\{p\}$ となるので $T\setminus\{p\}$ は開である。
$f$ は連続なので、閉集合の逆像 $f^{-1}(\{p\})$ は $S$ の閉部分集合である。
コンパクト空間の閉部分集合はコンパクトだから、$f^{-1}(\{p\})$ はコンパクトである。

$X=\Q^2$ について、$0<c<2$ を満たす無理数 $c$ を一つ取る（例えば $c=\sqrt2$）。
\[
U=X\cap\{(x,y)\in\R^2\mid x<c\},\qquad
V=X\cap\{(x,y)\in\R^2\mid x>c\}
\]
とおくと、二つの半平面は $\R^2$ の開集合なので $U,V$ は $X$ の相対位相で開である。
$(0,0)\in U$、$(2,0)\in V$ だからともに空でなく、
$x<c$ と $x>c$ は同時に成り立たないので $U\cap V=\varnothing$ である。
さらに $c$ は無理数だから $X$ の点の第1座標が $c$ に等しくなることはなく、
\[
X=U\sqcup V
\]
となる。従って $X$ は非連結である。

$Y=\R^2\setminus\Q^2$ は道連結であり、道連結な空間は連結なので、$Y$ は連結である。
道連結であることを示す。無理数 $\alpha,\beta$ を固定する。
$p=(p_1,p_2)\in Y$ について、$p_1$ が無理数なら
\[
p\longrightarrow(p_1,\beta)\longrightarrow(\alpha,\beta)
\]
という縦・横の線分を取り、$p_1$ が有理数なら $p_2$ は無理数なので
\[
p\longrightarrow(\alpha,p_2)\longrightarrow(\alpha,\beta)
\]
という横・縦の線分を取る。各線分上では少なくとも一方の座標が固定された無理数なので
$\Q^2$ と交わらない。任意の二点をそれぞれ $(\alpha,\beta)$ へ結んで一方を逆向きにつなげれば、
$Y$ 内の折れ線で結べる。
\end{proof}

\section{応用数理コース・専門基礎科目（4題必修）}

\begin{problem}{第1問｜連立方程式と行列}{2015-a-1}
連立方程式
$x+2y+3z=1$, $4x+5y+6z=-2$, $7x+8y+9z=a$ が解を持つ $a$ と全解を求めよ。また
\[
A=\begin{pmatrix}1&2&3\\2&3&1\\3&1&2\end{pmatrix},
B=\begin{pmatrix}1&2&3\\8&5&5\\3&1&2\end{pmatrix},
C=\begin{pmatrix}2&1&3\\3&2&1\\1&3&2\end{pmatrix}
\]
に対し $XA=B$, $AY=C$ を解け。さらに $A^2+A-2I=0$ なら $A$ が正則，冪零行列 $A$ は非正則だが $I\pm A$ は正則であることを示せ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
連立方程式では、第2式から第1式の4倍、第3式から第1式の7倍を引いて $x$ を消去する。
残った二式の左辺は定数倍の関係になるため、右辺も同じ倍率でなければ解が存在しない。

行列の積は一般に順序を交換できない。$XA=B$ から $X$ を孤立させるには右から
$A^{-1}$、$AY=C$ から $Y$ を孤立させるには左から $A^{-1}$ を掛ける。
その前に $|A|\ne0$ を確認し、余因子行列から逆行列を計算する。
最後の正則性では、逆行列の候補を多項式恒等式から直接作り、左右から掛けて単位行列になることを確認する。
\end{memo*}
\begin{proof}[解答]
第2式から第1式の4倍を引き、第3式から第1式の7倍を引くと
\[
-3y-6z=-6,\qquad -6y-12z=a-7.
\]
前者は $y+2z=2$ であり、後者がその2倍になるためには $a-7=-12$、
すなわち $a=-5$ が必要である。このとき $z=t$ とおけば
\[
y=2-2t,\qquad x=1-2y-3z=t-3,
\]
従って全解は $(x,y,z)=(t-3,2-2t,t)$ である。

$XA=B$ と $AY=C$ を解くため、まず $A$ の正則性を確認する。第1行で展開すると
\begin{align*}
|A|
&=1\begin{vmatrix}3&1\\1&2\end{vmatrix}
-2\begin{vmatrix}2&1\\3&2\end{vmatrix}
+3\begin{vmatrix}2&3\\3&1\end{vmatrix}\\
&=(6-1)-2(4-3)+3(2-9)\\
&=5-2-21=-18\ne0.
\end{align*}
従って $A$ は正則である。$A$ の余因子行列を $C_A=(C_{ij})$ とすると、各成分は
\[
C_A=
\begin{pmatrix}
\begin{vmatrix}3&1\\1&2\end{vmatrix}&
-\begin{vmatrix}2&1\\3&2\end{vmatrix}&
\begin{vmatrix}2&3\\3&1\end{vmatrix}\\[3mm]
-\begin{vmatrix}2&3\\1&2\end{vmatrix}&
\begin{vmatrix}1&3\\3&2\end{vmatrix}&
-\begin{vmatrix}1&2\\3&1\end{vmatrix}\\[3mm]
\begin{vmatrix}2&3\\3&1\end{vmatrix}&
-\begin{vmatrix}1&3\\2&1\end{vmatrix}&
\begin{vmatrix}1&2\\2&3\end{vmatrix}
\end{pmatrix}
=
\begin{pmatrix}5&-1&-7\\-1&-7&5\\-7&5&-1\end{pmatrix}.
\]
$\operatorname{adj}A$ は余因子行列の転置である。いまの $C_A$ は対称なので転置しても変わらず
\[
\operatorname{adj}A=
 C_A^{\mathsf T}
=\begin{pmatrix}5&-1&-7\\-1&-7&5\\-7&5&-1\end{pmatrix},
\]
よって
\[
A^{-1}=\frac1{|A|}\operatorname{adj}A
=\frac1{18}
\begin{pmatrix}-5&1&7\\1&7&-5\\7&-5&1\end{pmatrix}.
\]
$XA=B$ は右から $A^{-1}$ を掛けて $X=BA^{-1}$、
$AY=C$ は左から $A^{-1}$ を掛けて $Y=A^{-1}C$ である。それぞれの積は
\[
BA^{-1}=\frac1{18}
\begin{pmatrix}1&2&3\\8&5&5\\3&1&2\end{pmatrix}
\begin{pmatrix}-5&1&7\\1&7&-5\\7&-5&1\end{pmatrix}
=\frac1{18}
\begin{pmatrix}18&0&0\\0&18&36\\0&0&18\end{pmatrix}
=\begin{pmatrix}1&0&0\\0&1&2\\0&0&1\end{pmatrix},
\]
\[
A^{-1}C=\frac1{18}
\begin{pmatrix}-5&1&7\\1&7&-5\\7&-5&1\end{pmatrix}
\begin{pmatrix}2&1&3\\3&2&1\\1&3&2\end{pmatrix}
=\frac1{18}
\begin{pmatrix}0&18&0\\18&0&0\\0&0&18\end{pmatrix}
=\begin{pmatrix}0&1&0\\1&0&0\\0&0&1\end{pmatrix}.
\]
よって
\[
X=\begin{pmatrix}1&0&0\\0&1&2\\0&0&1\end{pmatrix},\qquad
Y=\begin{pmatrix}0&1&0\\1&0&0\\0&0&1\end{pmatrix}.
\]
以下、最後の二つの主張では $A$ を改めて一般の正方行列とする。
$A^2+A-2I=0$ とすると $A^2+A=2I$ であり、左辺を因数分解して
\[
A(A+I)=(A+I)A=2I,
\qquad\text{すなわち}\qquad
A\cdot\frac{A+I}2=\frac{A+I}2\cdot A=I
\]
となる。従って $A$ は正則で $A^{-1}=(A+I)/2$ である。

次に $A^m=O$ となる冪零行列 $A$ を考える。$A$ が正則だと仮定すると、
正則行列の積は正則なので $A^m$ も正則だが、$A^m=O$ は正則でないので矛盾する。
従って冪零行列は正則でない。一方、有限等比和を掛けると
\[
(I-A)(I+A+\cdots+A^{m-1})=I-A^m=I,
\]
\[
(I+A)(I-A+A^2-\cdots+(-A)^{m-1})=I-(-A)^m=I.
\]
従って $I-A$, $I+A$ はともに正則で、括弧内の行列がそれぞれの逆行列である。
\end{proof}

\begin{problem}{第2問｜対角化・漸化式・直交行列}{2015-a-2}
$A=\begin{pmatrix}-2&1&2\\1&0&0\\0&1&0\end{pmatrix}$ の固有値・固有空間を求め，対角化せよ。対角化に使う正則行列とその逆行列，$A^n$ の第1行も求めよ。また $x_n=-2x_{n-1}+x_{n-2}+2x_{n-3}$ の二組の指定初期値 $(1,0,0)$ と $(0,-1,1)$ に対する一般項を求めよ。

さらに，実正方行列 $B$ に関する次の三条件が同値であることを示せ。
\begin{enumerate}
  \item[(ア)] $B$ は直交行列である。
  \item[(イ)] 任意の $x\in\R^n$ に対し $\norm{Bx}=\norm{x}$。
  \item[(ウ)] 任意の $x,y\in\R^n$ に対し $\inner{Bx}{By}=\inner{x}{y}$。
\end{enumerate}
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
まず $|\lambda I-A|=0$ を因数分解して固有値を求め、
各 $\lambda$ を $(A-\lambda I)x=0$ へ代入して固有ベクトルを得る。
固有ベクトルを列に並べた $P$ では $AP=PD$ が成り立つため、左から $P^{-1}$ を掛けて
$P^{-1}AP=D$、さらに $A^n=PD^nP^{-1}$ とできる。

漸化式へ試行解 $x_n=r^n$ を代入すると、行列と同じ三根 $1,-1,-2$ を持つ特性方程式が現れる。
根が相異なるので一般解を三つの指数列の線形結合として置き、初期値を代入して係数を解く。
直交行列の同値条件では、$\norm{x}^2=\inner{x}{x}$ と
$\norm{x+y}^2$ の展開を使い、「行列条件・長さ・内積」を順につなぐ。
\end{memo*}
\begin{proof}[解答]
固有値を求めるため特性多項式を計算すると
\begin{align*}
|\lambda I-A|
&=\begin{vmatrix}\lambda+2&-1&-2\\-1&\lambda&0\\0&-1&\lambda\end{vmatrix}\\
&=(\lambda+2)\lambda^2-\lambda-2\\
&=(\lambda-1)(\lambda+1)(\lambda+2).
\end{align*}
従って固有値は $1,-1,-2$ である。
$x=(x_1,x_2,x_3)^{\mathsf T}$ に対して $Ax=\lambda x$ を成分で書くと
\[
\begin{cases}
-2x_1+x_2+2x_3=\lambda x_1,\\
x_1=\lambda x_2,\\
x_2=\lambda x_3.
\end{cases}
\]
今回の固有値はいずれも0でない。第2、第3式から
\[
x_2=\lambda x_3,
\qquad x_1=\lambda x_2=\lambda^2x_3
\]
となる。$x_3=t$ とおけば
\[
x=t(\lambda^2,\lambda,1)^{\mathsf T}.
\]
$\lambda=1,-1,-2$ を順に代入して
\[
E_1=\langle(1,1,1)^{\mathsf T}\rangle,\quad
E_{-1}=\langle(1,-1,1)^{\mathsf T}\rangle,\quad
E_{-2}=\langle(4,-2,1)^{\mathsf T}\rangle.
\]
三つの固有値が相異なるので、これらの固有ベクトルは一次独立であり、
\[
P=\begin{pmatrix}1&1&4\\1&-1&-2\\1&1&1\end{pmatrix}
\]
とおける。実際
\[
|P|
=1\begin{vmatrix}-1&-2\\1&1\end{vmatrix}
-1\begin{vmatrix}1&-2\\1&1\end{vmatrix}
+4\begin{vmatrix}1&-1\\1&1\end{vmatrix}
=1-3+8=6\ne0
\]
なので $P$ は正則である。余因子を用いると
\[
P^{-1}=\frac1{6}
\begin{pmatrix}1&3&2\\-3&-3&6\\2&0&-2\end{pmatrix}
=\begin{pmatrix}1/6&1/2&1/3\\-1/2&-1/2&1\\1/3&0&-1/3\end{pmatrix}.
\]
$P$ の各列は順に固有値 $1,-1,-2$ の固有ベクトルだから
\[
AP=P\diag(1,-1,-2).
\]
左から $P^{-1}$ を掛けて
\[
P^{-1}AP=\diag(1,-1,-2)
\]
を得る。

従って
\[
A^n=P\diag(1,(-1)^n,(-2)^n)P^{-1}.
\]
右辺の第1行を求めるため、まず
\[
(1,1,4)\diag(1,(-1)^n,(-2)^n)
=\bigl(1,(-1)^n,4(-2)^n\bigr)
\]
と計算する。これを $P^{-1}$ に掛けると、$A^n$ の第1行は
\[
\left(\frac16-\frac{(-1)^n}2+\frac43(-2)^n,
\frac{1-(-1)^n}2,
\frac13+(-1)^n-\frac43(-2)^n\right).
\]

漸化式の特性方程式も
\[
r^3+2r^2-r-2=(r-1)(r+1)(r+2)=0
\]
なので一般解は $x_n=\alpha+\beta(-1)^n+\gamma(-2)^n$ と書ける。
$n=1,2,3$ を代入すると
\[
x_1=\alpha-\beta-2\gamma,
\qquad
x_2=\alpha+\beta+4\gamma,
\qquad
x_3=\alpha-\beta-8\gamma.
\]

初期値 $(x_1,x_2,x_3)=(1,0,0)$ のときは
\[
\begin{cases}
\alpha-\beta-2\gamma=1,\\
\alpha+\beta+4\gamma=0,\\
\alpha-\beta-8\gamma=0.
\end{cases}
\]
第3式から第1式を引くと $-6\gamma=-1$ だから $\gamma=1/6$ である。
第2式から第1式を引くと
$2\beta+6\gamma=-1$ だから $\beta=-1$ となり、第1式から $\alpha=1/3$ を得る。

初期値 $(x_1,x_2,x_3)=(0,-1,1)$ のときは
\[
\begin{cases}
\alpha-\beta-2\gamma=0,\\
\alpha+\beta+4\gamma=-1,\\
\alpha-\beta-8\gamma=1.
\end{cases}
\]
第3式から第1式を引くと $-6\gamma=1$ だから $\gamma=-1/6$ である。
第2式から第1式を引くと
$2\beta+6\gamma=-1$ だから $\beta=0$ となり、第1式から $\alpha=-1/3$ を得る。
よって一般項はそれぞれ
\[
x_n=\frac13-(-1)^n+\frac16(-2)^n,\qquad
x_n=-\frac13-\frac16(-2)^n.
\]

最後に三条件の同値を示す。
$(\text{ア})$、すなわち $B^{\mathsf T}B=I$ を仮定すると、任意の $x\in\R^n$ に対して
\[
\norm{Bx}^2
=(Bx)^{\mathsf T}(Bx)
=x^{\mathsf T}B^{\mathsf T}Bx
=x^{\mathsf T}x
=\norm{x}^2.
\]
両辺は非負なので平方根を取り $\norm{Bx}=\norm{x}$ となる。よって
$(\text{ア})\Rightarrow(\text{イ})$ である。

次に (イ) を仮定する。$B$ の線形性と (イ) を $x+y,x,y$ に適用すると
\begin{align*}
\norm{x+y}^2
&=\norm{B(x+y)}^2\\
&=\norm{Bx+By}^2\\
&=\norm{Bx}^2+2\inner{Bx}{By}+\norm{By}^2\\
&=\norm{x}^2+2\inner{Bx}{By}+\norm{y}^2.
\end{align*}
一方
\[
\norm{x+y}^2
=\norm{x}^2+2\inner{x}{y}+\norm{y}^2
\]
だから、共通する項を消去して
\[
\inner{Bx}{By}=\inner{x}{y}
\]
を得る。よって $(\text{イ})\Rightarrow(\text{ウ})$ である。

最後に (ウ) を仮定し、$e_1,\ldots,e_n$ を $\R^n$ の標準基底とする。
$B^{\mathsf T}B$ の $(i,j)$ 成分は
\[
e_i^{\mathsf T}B^{\mathsf T}Be_j
=(Be_i)^{\mathsf T}(Be_j)
=\inner{Be_i}{Be_j}
=\inner{e_i}{e_j}
=\delta_{ij}.
\]
従って $B^{\mathsf T}B=I$ であり、$B$ は直交行列である。
よって $(\text{ウ})\Rightarrow(\text{ア})$ も成り立ち、三条件は同値である。
\end{proof}

\begin{problem}{第3問｜初等微積分}{2015-a-3}
$\log|\tan x|$ と $x^{\sin x}$ の導関数を求めよ。$x+\sqrt{x^2+1}=t$ から $x$ を表し，$\int_0^1\d x/\sqrt{x^2+1}$ と $\int_0^1\sqrt{x^2+1}\,\d x$ を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
$\log|\tan x|$ は $\tan x\ne0$ の区間ごとに、合成関数の微分公式
$(\log|u|)'=u'/u$ を使う。$x^{\sin x}$ は実数値として $x>0$ で考え、
$x^{\sin x}=\exp(\sin x\log x)$ と書いて指数関数と積の微分へ直す。

置換 $t=x+\sqrt{x^2+1}$ では、共役な式を掛けると
\[
(\sqrt{x^2+1}+x)(\sqrt{x^2+1}-x)=1
\]
となるため、$t^{-1}=\sqrt{x^2+1}-x$ が得られる。
$t$ と $t^{-1}$ を加減して $x$ と平方根を表し、$\d x$ と積分端点も全て $t$ へ変換する。
\end{memo*}
\begin{proof}[解答]
まず導関数を求める。$\tan x\ne0$ となる区間で、合成関数の微分公式
$(\log|u|)'=u'/u$ を $u=\tan x$ に使うと
\[
\frac{\d}{\d x}\log|\tan x|
=\frac{(\tan x)'}{\tan x}
=\frac{\sec^2x}{\tan x}
=\frac{1/\cos^2x}{\sin x/\cos x}
=\frac1{\sin x\cos x}.
\]
$x^{\sin x}$ は $x>0$ で実数値として定まり、$x^{\sin x}=\exp(\sin x\log x)$ と書ける。
指数関数の微分と積の微分公式を順に使うと
\begin{align*}
\frac{\d}{\d x}x^{\sin x}
&=\exp(\sin x\log x)\cdot\frac{\d}{\d x}(\sin x\log x)\\
&=x^{\sin x}\left(\cos x\log x+\frac{\sin x}{x}\right).
\end{align*}

次に置換を作る。$t=x+\sqrt{x^2+1}$ に対して
\[
(\sqrt{x^2+1}+x)(\sqrt{x^2+1}-x)=(x^2+1)-x^2=1
\]
なので $t^{-1}=\sqrt{x^2+1}-x$ である。この二式を辺々加減すると
\[
t-t^{-1}=2x,\qquad t+t^{-1}=2\sqrt{x^2+1},
\]
すなわち
\[
x=\frac{t-t^{-1}}2,\qquad
\sqrt{x^2+1}=\frac{t+t^{-1}}2,\qquad
\d x=\frac{1+t^{-2}}2\,\d t.
\]
最後の式は $x=(t-t^{-1})/2$ を $t$ で微分したものである。
また $x=0$ は $t=0+1=1$、$x=1$ は $t=1+\sqrt2$ に対応する。

一つ目の積分では、$t+t^{-1}=t(1+t^{-2})$ に注意すると被積分要素が
\[
\frac{\d x}{\sqrt{x^2+1}}
=\frac{2}{t+t^{-1}}\cdot\frac{1+t^{-2}}2\,\d t
=\frac{1+t^{-2}}{t(1+t^{-2})}\,\d t
=\frac{\d t}{t}
\]
と簡単になる。従って
\[
\int_0^1\frac{\d x}{\sqrt{x^2+1}}
=\int_1^{1+\sqrt2}\frac{\d t}{t}
=\left[\log t\right]_1^{1+\sqrt2}
=\log(1+\sqrt2).
\]
二つ目の積分では
\[
(t+t^{-1})(1+t^{-2})=t+t^{-1}+t^{-1}+t^{-3}=t+2t^{-1}+t^{-3}
\]
だから
\begin{align*}
\int_0^1\sqrt{x^2+1}\,\d x
&=\int_1^{1+\sqrt2}\frac{t+t^{-1}}2\cdot\frac{1+t^{-2}}2\,\d t\\
&=\frac14\int_1^{1+\sqrt2}(t+2t^{-1}+t^{-3})\,\d t\\
&=\frac14\left[\frac{t^2}{2}+2\log t-\frac1{2t^2}\right]_1^{1+\sqrt2}\\
&=\left[\frac{t^2}{8}+\frac12\log t-\frac1{8t^2}\right]_1^{1+\sqrt2}.
\end{align*}
ここで $t=1+\sqrt2$ では $t^2=3+2\sqrt2$ であり、
$(3+2\sqrt2)(3-2\sqrt2)=9-8=1$ より $t^{-2}=3-2\sqrt2$ だから
\[
\frac{t^2}{8}-\frac1{8t^2}
=\frac{(3+2\sqrt2)-(3-2\sqrt2)}{8}
=\frac{4\sqrt2}{8}=\frac{\sqrt2}{2}.
\]
$t=1$ では $1/8+0-1/8=0$ である。従って
\[
\int_0^1\sqrt{x^2+1}\,\d x
=\frac{\sqrt2}{2}+\frac12\log(1+\sqrt2)
=\frac{\sqrt2+\log(1+\sqrt2)}2.
\]
\end{proof}

\begin{problem}{第4問｜最大最小と三重積分}{2015-a-4}
$\sqrt x+\sqrt y=1$ のもとで $x^2+xy+y^2$ の最大・最小を求めよ。また原点中心・半径 $a$ の球 $V$ 上で $\iiint_V(x+y+z)\,\d x\d y\d z$ を求めよ。
\end{problem}
\begin{memo*}
\textbf{解答の見通し。}
平方根が現れるので $u=\sqrt x$, $v=\sqrt y$ と置く。このとき $x=u^2$, $y=v^2$、
$u,v\ge0$ で、制約は直線 $u+v=1$ になる。
目的関数を展開し、$u+v=1$ を使って積 $q=uv$ だけの式へ直す。
相加相乗平均から $0\le q\le1/4$ なので、一変数二次関数の増減として最大・最小を決める。

三重積分では、球内の各点 $(x,y,z)$ と反対側の点 $(-x,-y,-z)$ が対になり、
領域の体積要素は変わらない一方、$x+y+z$ の符号だけが反転する。
変数変換を式にすると積分値 $J$ が $J=-J$ を満たし、$J=0$ となる。
\end{memo*}
\begin{proof}[解答]
制約 $\sqrt x+\sqrt y=1$ では平方根が現れるので、$u=\sqrt x$, $v=\sqrt y$ とおく。
このとき $x=u^2$, $y=v^2$ で、$u,v\ge0$ かつ $u+v=1$ である。
$q=uv$ とおくと、相加相乗平均より
\[
q=uv\le\left(\frac{u+v}2\right)^2=\frac14
\]
であり、$u,v\ge0$ から $q\ge0$ でもある。逆に $0\le q\le1/4$ なら
$u,v$ は2次方程式 $s^2-s+q=0$ の二根として実際に取れる（判別式は $1-4q\ge0$）。
従って $q$ の動く範囲はちょうど $[0,1/4]$ である。目的関数は
\begin{align*}
x^2+xy+y^2
&=u^4+u^2v^2+v^4\\
&=(u^2+v^2)^2-u^2v^2\\
&=\{(u+v)^2-2uv\}^2-(uv)^2\\
&=(1-2q)^2-q^2\\
&=1-4q+3q^2=:F(q)
\end{align*}
と $q$ だけの式になる。$F'(q)=-4+6q$ は $0\le q\le1/4$ では
$6q\le3/2<4$ より常に負なので、$F$ はこの区間で単調減少する。
従って最大値は $q=0$ で取る $F(0)=1$ であり、$q=uv=0$ は
$(u,v)=(1,0),(0,1)$、すなわち $(x,y)=(1,0),(0,1)$ に対応する。
最小値は $q=1/4$ で取る
\[
F\left(\frac14\right)=1-1+\frac3{16}=\frac3{16}
\]
であり、$q=1/4$ は $u=v=1/2$、すなわち $x=y=1/4$ に対応する。

三重積分では、原点に関する点対称移動
\[
T(x,y,z)=(-x,-y,-z)
\]
を変数変換として使う。$T$ は線形写像で、その行列は $-I_3$ だから
\[
\frac{\partial(-x,-y,-z)}{\partial(x,y,z)}=|-I_3|=(-1)^3=-1,
\qquad
\left|\frac{\partial(-x,-y,-z)}{\partial(x,y,z)}\right|=1
\]
であり、体積要素は変わらない。また $V$ は原点中心の球なので
$(x,y,z)\in V$ と $T(x,y,z)\in V$ は同値であり、$T$ は $V$ を $V$ へ写す。
積分値を $J$ とおくと、変数変換により
\begin{align*}
J&=\iiint_V(x+y+z)\,\d x\d y\d z\\
&=\iiint_V\{(-x)+(-y)+(-z)\}\,\d x\d y\d z\\
&=-J
\end{align*}
となる。従って $2J=0$、すなわち $J=0$ である。
\end{proof}
